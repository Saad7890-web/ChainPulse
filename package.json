{
  "name": "chainpulse",
  "private": true,
  "workspaces": [
    "services/*",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:shipment": "npm --workspace services/shipment-service run dev"
  }
}
